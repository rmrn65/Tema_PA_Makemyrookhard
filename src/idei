Set pieseInJoc = {...};
ArrayList pieseCarePotMisca
foreach(Set) {
    if(piece.canMove)
       pieseCarePotMisca.add(piece); 
}
Random random() -> random pe arraylist
piece.move();

Piece -> boolean canMove(Board board) -> generateMoves();
      -> String move(Board board)


